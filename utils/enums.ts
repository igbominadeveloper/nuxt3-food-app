enum NavigationLink {
  Instructions = 'instructions',
  Ingredients = 'ingredients',
}

enum Measure {
  US = 'us',
  Metric = 'metric',
}

enum InjectKey {
  Measure = 'measure',
}

export { InjectKey, NavigationLink, Measure };
